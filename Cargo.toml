[package]
name = "to-do-list"
version = "0.1.0"
edition = "2024"

[dependencies]
# serde is a framework for serializing and deserializing data
serde = { version = "1.0", features = ["derive"] }

# serde_json is a JSON implementation for serde
serde_json = "1.0"

